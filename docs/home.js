import { div, p } from './fntags.js'

export default div(
    p( 'With fntags, you write js instead of html.' ),
    p( 'There\'s no complicated set up, and no build process.' ),
    p( 'Dom element objects are created using functions that take an optional attributes object and the elements children.' ),
    p( 'These functions are used to compose templates and re-usable components.' )
)